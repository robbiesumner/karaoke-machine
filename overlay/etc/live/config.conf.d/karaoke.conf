# live-config tunables for karaoke-machine.
# Replaces the default `user` (uid 1000) with `karaoke`.
LIVE_USERNAME="karaoke"
LIVE_USER_FULLNAME="karaoke-machine"
LIVE_USER_DEFAULT_GROUPS="audio,video,input,plugdev,render,cdrom,floppy"
LIVE_HOSTNAME="karaoke-machine"
